# Real backend (default): leave both variables unset. Server components use the
# in-cluster gateway and browser requests stay on the same-origin /api route.
#
# Prism development mock:
PRISM_MOCK_ENABLED=true
API_BASE_URL=http://localhost:4010
#
# Development-only authenticated-session bypass:
DEV_AUTH_BYPASS=true
